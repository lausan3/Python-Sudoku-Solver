class SudokuSolver:
    BOARD_SIZE = 9
    board = [BOARD_SIZE][BOARD_SIZE]
    
    def __init__():
        for row in range(BOARD_SIZE):
            for col in range(BOARD_SIZE):

        solvable = False